
[project]
name = "NiMP"
dynamic = ["version"]
description = "Python wrapper for NiMP, container based web development environment."
authors = [
    { name = "Nox", email = "nox@pm.me" }
]
license = { file = "LICENSE.md" }
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.9"
dependencies = [
    "docker~=7.1.0",
    "python-dotenv~=1.1.1",
    "GitPython~=3.1.43",
    "argcomplete~=3.5.0",
]